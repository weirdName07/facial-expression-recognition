# AI Facial Expression Platform - Environment Configuration
# Copy this to .env and adjust variables as needed.

# ── Backend Configuration ──
API_PORT=8000
REDIS_HOST=redis
REDIS_PORT=6379

# Camera Index (usually 0 for built-in, 1 for external)
# Note: Camera access in Docker usually requires device passthrough (Linux) 
# or specific drivers (Windows/macOS).
CAMERA_INDEX=0

# Throttling Settings
BIOMETRIC_THROTTLE_FRAMES=30
FPS_TARGET=15

# ── Frontend Configuration ──
# (Angular typically bakes these in during build or uses runtime config)
FRONTEND_PORT=4200
WS_GATEWAY_URL=ws://localhost:8000/ws/stream
